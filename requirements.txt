# ================================================================
# DEPLOYMENT: Streamlit Cloud (Python 3.10+)
# This is the minimal set needed for the DASHBOARD ONLY
# Training dependencies should be in a separate requirements-dev.txt
# ================================================================

# Core Dashboard
streamlit==1.28.1
pandas==2.1.0
plotly==5.17.0

# Data Processing
numpy==1.24.3

# Utilities
python-dotenv==1.0.0

# ================================================================
# IMPORTANT: For LOCAL DEVELOPMENT (not deployed):
# If you want to train models locally, install from requirements-dev.txt
# But DO NOT include training packages in deployment!
# ================================================================
